<% if @ds_comment %>
<br/>
  <div class="comment-show">
    "<%= @ds_comment.comment %>" By <%= User.find_by_id(@ds_comment.commented_by).try(:name)%>- On <%= @ds_comment.created_at %>
  </div>
<br/>
<% end %>

<script>
  $('.comment-show').mouseup(function(event){
    event.stopPropagation();
  });
</script>

